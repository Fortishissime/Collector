import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    mode: 'dark', 
    primary: {
      main: '#1d4ed8', 
    },
    secondary: {
      main: '#64748b',
    },
  },
});

export default theme;
